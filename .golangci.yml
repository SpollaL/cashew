run:
  timeout: 5m

linters:
  enable:
    - errcheck       # unchecked errors
    - staticcheck    # comprehensive static analysis (replaces deprecated megacheck)
    - govet          # suspicious constructs (shadowed vars, printf mismatches)
    - ineffassign    # assignments whose value is never used
    - unused         # unexported code that is never referenced
    - misspell       # common English spelling mistakes in comments/strings

issues:
  exclude-rules:
    # errcheck is too noisy in tests (e.g. ignoring error from deferred Close)
    - path: _test\.go
      linters:
        - errcheck
